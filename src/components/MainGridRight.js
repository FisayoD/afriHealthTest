import React from "react";

export default class MainGridRight extends React.Component {
    render() {
        return (
            <div>
                {this.props.children}
            </div>
        );
    }
}
